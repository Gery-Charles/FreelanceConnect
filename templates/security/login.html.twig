{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

        {%  block header_content %}
            {% if error %}
                <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}

            <form method="post">
                <h2 class="h3 mb-3 font-weight-normal">Connexion</h2>
                <p><label for="username">Email</label></p>
                <input type="email" placeholder="Email" name="username" id="username" class="form-control" required autofocus>

                <p><label for="password">Mot de passe</label></p>
                <input type="password" placeholder="Mot de passe" name="password" id="password" class="form-control" required>

                <input type="hidden" name="_csrf_token"
                       value="{{ csrf_token('authenticate') }}"
                >

                {#
                Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
                See https://symfony.com/doc/current/security/remember_me.html

                <div class="checkbox mb-3">
                    <input type="checkbox" name="_remember_me" id="_remember_me">
                    <label for="_remember_me">Remember me</label>
                </div>
                #}

                <br>
                <button class="btn btn-lg btn-primary" type="submit">
                    Valider
                </button>
            </form>
        {% endblock %}



